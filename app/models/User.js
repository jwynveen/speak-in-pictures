'use strict';

module.exports = {
  // tableName: 'user',
  // identity: 'user',
  // schema: true,
  // autoUpdatedAt: true,
  // autoCreatedAt: true,

  attributes: {
    name: {
      type: 'string',
    },
  },
};
